import { faq } from "../data/faq.js"; import SectionTitle from "../ui/SectionTitle.js"; export default function FAQ() { return ` <section class="faq-section reveal"> <div class="container"> ${SectionTitle( "Frequently Asked Questions", "Answers to the most common questions from workers and employers." )} <div class="faq-list"> ${faq.map(item => ` <details class="faq-item"> <summary>${item.question}</summary> <p>${item.answer}</p> </details> `).join("")} </div> </div> </section> `; }